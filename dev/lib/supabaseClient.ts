import { createClient } from "@supabase/supabase-js";

export const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);

/**
 * ======================================================
 * Supabase Client 초기화
 * ======================================================
 * 📌 사용 목적
 * ------------------------------------------------------
 * - 모든 DB 액세스를 supabase-js SDK를 통해 수행하기 위한
 *   클라이언트 인스턴스 생성
 *
 * 환경 변수
 * ------------------------------------------------------
 * - NEXT_PUBLIC_SUPABASE_URL
 * - NEXT_PUBLIC_SUPABASE_ANON_KEY
 *
 * 📌 주의사항
 * ------------------------------------------------------
 * - 환경 변수는 반드시 `.env.local`에서 설정되어야 함
 * - 클라이언트 키는 Public Key이므로 브라우저에서 사용 가능
 * - 보안 민감 로직(권한 검증 등)은 반드시 백엔드에서 처리
 *
 * 사용 위치
 * ------------------------------------------------------
 * - lib/*Service.ts 파일에서 데이터베이스 조작 시 사용
 * ======================================================
 */
