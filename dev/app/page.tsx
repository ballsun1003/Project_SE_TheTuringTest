import HomePage from "./home/page";
export default function RootPage() {
  return <HomePage />;
}
/**
 * ======================================================
 * RootPage (app/page.tsx)
 * ======================================================
 * 루트 경로("/")에 접근했을 때 HomePage 컴포넌트를 렌더링하는
 * Next.js 라우팅 연결용 컴포넌트.
 *
 * 기능
 * ------------------------------------------------------
 * - "/home" 경로에 정의된 HomePage를 기본 페이지로 사용
 * - 루트 → 홈 페이지 자동 매핑
 *
 * 목적
 * ------------------------------------------------------
 * - 프로젝트 최상단 URL 접근 시 홈 화면으로 이동시키기 위함
 * - 메인 진입점 역할 수행
 * ======================================================
 */
